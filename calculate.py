# Write a function calculate which is passed
# two operands and an operator and returns
# the calculated result, e.g., calculate(2, 4, '+') would return 6
def calculate(a, b):
   return a + b

result = calculate(2, 4)
print(result)